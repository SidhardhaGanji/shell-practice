#!/bin/bash          # Shebang → tells Linux to use Bash interpreter

DATE=$(date)         # Runs the `date` command and stores its output in variable DATE

echo "Time stamp executed: $DATE"   # Prints the message with the current date/time
